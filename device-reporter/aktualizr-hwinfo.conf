# Make aktualizr publish our enriched report as system_info. Explicit ExecStart (not the
# $AKTUALIZR_CMDLINE_PARAMETERS env var, whose word-splitting drops the file argument).
[Service]
ExecStart=
ExecStart=/usr/bin/aktualizr-torizon --hwinfo-file /var/sota/hwinfo.json
